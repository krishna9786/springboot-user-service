version: '3.8'

services:
  user-service:
    build: .
    image: user-service:latest
    container_name: user-service-container
    ports:
      - "8080:8080"
    environment:
      # Pass your AWS RDS PostgreSQL credentials as environment variables here
      - SPRING_DATASOURCE_URL=jdbc:postgresql://userdb.cf48kysi2tk5.eu-north-1.rds.amazonaws.com:5432/postgres
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=baba978666
    restart: "no"